[tool.poetry]
name = "workspace"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
streamlit = ">=1.43.2"
pyarrow = ">=19.0.1"
plotly = ">=6.0.1"
pandas = ">=2.2.3"
python-jose = {version = ">=3.4.0", extras = ["pycryptodome"]}
bcrypt = ">=4.3.0"
flask = ">=2.0.1"
sqlitecloud = ">=0.0.83"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"